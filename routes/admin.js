const express = require('express')
const router = express.Router()

// Rotas ADMIN
// router.get('/', (req, res) => {
//   Usuario.findAll().then(function(usuarios){
//     res.render("admin/cadastros", {nome: 'tamara', sobrenome: 'montijo'})
//   })
// })

module.exports = router
